# Tests/Support — shared iOS test helpers

Quick map:

- `RecordingURLProtocol.swift` — engine network stub + session installer
- `TestModelContainers.swift` — in-memory container + on-disk restart + legacy-fixture open
- `TestLegacyStoreFixtures.swift` — #220 Slice B wrappers over #215 `LegacyStoreFixtures`
